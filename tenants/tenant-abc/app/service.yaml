apiVersion: v1
kind: Service
metadata:
  name: bucket-api
  namespace: tenant-abc
spec:
  selector:
    app: bucket-api
  ports:
    - port: 80
      targetPort: 8000
  type: NodePort
  imagePullSecrets:
    - name: tenant-abc-gcp-sa